Jenkinsfile (Declarative Pipeline)
pipline { 
    agent {dockerfile true} 
    stages{ 
        stage('Test') {
            steps{ 
                sh 'node --version'
                sh 'Look at me ma, no hands!'
            }
        }
    }
}
